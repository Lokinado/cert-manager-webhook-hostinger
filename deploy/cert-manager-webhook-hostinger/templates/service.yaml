apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-webhook-hostinger.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "cert-manager-webhook-hostinger.name" . }}
    chart: {{ include "cert-manager-webhook-hostinger.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "cert-manager-webhook-hostinger.name" . }}
    release: {{ .Release.Name }}
